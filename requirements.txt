# nmos-reference node — runtime dependencies (Python 3.12+)
#
# Pinned to the versions validated in this build. Install with:
#     python3.12 -m venv venv && . venv/bin/activate
#     pip install -r requirements.txt
#
# Note: cryptography, pycryptodome and aiohttp install platform-specific
# compiled wheels (no pure-Python build exists for cryptography/pycryptodome),
# so first install needs pip + internet to fetch the right wheel per OS/arch.
# Transitive deps (multidict, yarl, frozenlist, aiosignal, propcache,
# aiohappyeyeballs, attrs, MarkupSafe, cffi) are resolved automatically by pip.
aiohttp==3.14.1
cryptography==48.0.0
Jinja2==3.1.6
pycryptodome==3.23.0
PyJWT==2.13.0
pyOpenSSL==26.2.0
